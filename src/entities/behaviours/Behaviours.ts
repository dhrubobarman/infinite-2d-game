import type { Enemies } from '@/entities/Enemies';
import type { Player } from '@/entities/Player';

export class Behaviours {
  update(enemy: Enemies, dt: number, player: Player) {
    void enemy;
    void dt;
    void player;
  }
  reset() {}
}
